version: '3.8'

services:
  document-rag-assistant:
    build: .
    ports:
      - "8501:8501"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      # Mount for persistent uploads (optional)
      - ./uploads:/app/uploads

    restart: always